import { HideForRole } from './hide-for-role';

describe('HideForRole', () => {
  it('should create an instance', () => {
    const directive = new HideForRole();
    expect(directive).toBeTruthy();
  });
});
